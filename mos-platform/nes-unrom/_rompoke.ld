/**
 * Automatically generated by generate-rompoke-mapper-linkscript.lua.
 *
 * This script generates the ROM poke table size (nes/misc/rompoke/README.md)
 * for writes of maximum values determined by the following bit field:
 *
 * bits 0-7: PRG ROM bank (linker expression: __prg_rom_size)
 */
__rom_poke_table_size = (__submapper == 1) ? 0 : ((__prg_rom_size > 2048) ? 256 : ((__prg_rom_size > 1024) ? 128 : ((__prg_rom_size > 512) ? 64 : ((__prg_rom_size > 256) ? 32 : ((__prg_rom_size > 128) ? 16 : ((__prg_rom_size > 64) ? 8 : ((__prg_rom_size > 32) ? 4 : ((__prg_rom_size > 16) ? 2 : (0)))))))));
